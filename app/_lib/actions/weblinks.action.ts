"use server"

import { weblinksMock } from "../mock-data/weblinks-data"

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
export const getCachedWeblinksAction = async () => {
  return weblinksMock
}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
export const getWeblinkByIdAction = async (id: string) => {
  return weblinksMock.filter(w => w._id === id)[0]
}